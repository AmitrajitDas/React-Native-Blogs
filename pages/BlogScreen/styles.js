import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    padding: 20,
  },

  title: {
    marginTop: 20,
    fontSize: 30,
    fontWeight: "bold",
  },

  body: {
    fontSize: 20,
    marginTop: 20,
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
})
